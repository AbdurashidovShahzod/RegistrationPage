<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uz.pdp.registrationpage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RegistrationPage">
        <activity android:name=".contact.ThreeContactActivity"></activity>
        <activity android:name=".contact.SecondContactActivity" />
        <activity android:name=".contact.FirstContactActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".ui.MainActivity2" />
        <activity android:name=".ui.LoginThreeActivity" />
        <activity android:name=".ui.LoginSecondActivity" />
        <activity android:name=".ui.LoginFirstActivity">

        </activity>
        <activity android:name=".ui.MainActivity" >

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>